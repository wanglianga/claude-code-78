# 宿主机对外发布端口（内部固定 4000，切勿在并行任务中硬编码）
CC_PUBLISH_PORT=3078
COMPOSE_PROJECT_NAME=task2026-09-1578

# 应用配置（容器内）
PORT=4000
DATA_DIR=/data
# 晚接规定时间，超过该时间未接走触发晚接预警
PICKUP_DEADLINE=17:30
# 空库首次启动是否写入演示数据
SEED_ON_EMPTY=true
